rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes during development
    // TODO: Implement proper security rules before production deployment
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
